- Para testar o código, rode "Trabalho02Application" que se é a REST API
- Em seguida, dentro de interface/devweb, rode "npm install" e em depois "npm run dev"

#### Sobre as entregas
- Trabalho04 se refere a branch main
